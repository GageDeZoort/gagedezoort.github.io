---
import Layout from '../layouts/Layout.astro';

const pageTitle = 'Gage DeZoort · Particle Physics & Machine Learning';
const description =
	'Postdoctoral researcher at Princeton designing graph neural network methods for high-energy physics and precision discovery.';

const highlights = [
	{
		kicker: 'JHEP · 2025',
		title: 'Heavy pseudoscalar Higgs search (A → Zh → ℓℓττ)',
		url: 'https://doi.org/10.1007/JHEP10%282025%29074',
		description:
			'Search for a pseudoscalar Higgs boson A decaying to Zh with ℓℓττ final states in the full CMS Run 2 dataset.',
	},
	{
		kicker: 'Nature Reviews · 2023',
		title: 'Graph Neural Networks at the Large Hadron Collider',
		url: 'https://doi.org/10.1038/s42254-023-00569-0',
		description:
			'A survey of graph neural networks and the LHC physics tasks they enable, written for both physicists and data scientists.',
	},
	{
		kicker: 'SIMODS · 2025',
		title: 'Principles for Initialization and Architecture Selection in GNNs',
		url: 'https://doi.org/10.1137/23M1600621',
		description:
			'Principles for developing numerically stable GNNs that train to arbitrary depth and width, with full characterization of initialization and smoothing control.',
	},
];

const connections = [
	{ label: 'Email', href: 'mailto:jdezoort@princeton.edu' },
	{ label: 'GitHub', href: 'https://github.com/GageDeZoort' },
	{ label: 'Twitter/X', href: 'https://x.com/GDezoort' },
	{ label: 'Substack', href: 'https://gagedezoort.substack.com/' },
	{ label: 'CV', href: '/docs/Gage-DeZoort-CV.pdf' },
];

const tutorialLinks = [
	{
		label: 'Crash Course in Practical ML · UC Irvine (2025)',
		href: 'https://github.com/GageDeZoort/intro_ml_uci',
	},
	{
		label: 'Graph Neural Networks for Your Research · Princeton Research Computing (2023)',
		href: 'https://github.com/GageDeZoort/prc_gnn_tutorial',
	},
];

const instagramLink = 'https://www.instagram.com/gagedezoort.music/';
---

<Layout title={pageTitle} description={description}>
	<header class="hero">
		<div class="hero__text">
			<h1 class="hero__heading">
				<span class="hero__name">Gage DeZoort</span>
				<span aria-hidden="true" class="hero__dot">·</span>
				<span class="hero__dept">Princeton ORFE / Physics</span>
			</h1>
			<p class="hero__lede">
				Welcome! I'm a postdoctoral researcher building efficient, scalable machine learning algorithms for scientific
				measurements. Right now I'm focused on numerically stable graph neural networks tuned for hyperparameter
				transfer—models that agree on a single learning rate—so they can power tasks like stellar stream identification in
				large sky surveys and jet tagging in collider datasets.
			</p>
			<div class="hero__actions">
				<a class="button button--primary" href="/docs/Gage-DeZoort-CV.pdf" target="_blank" rel="noreferrer">
					Curriculum Vitae
				</a>
				<a class="button button--ghost" href="mailto:jdezoort@princeton.edu">
					Contact
				</a>
			</div>
		</div>
		<div class="hero__portrait">
			<div class="hero__frame">
				<img src="/images/headshot.jpg" alt="Portrait of Gage DeZoort" loading="lazy" decoding="async" />
			</div>
		</div>
	</header>

	<main class="content">
		<section class="section-card about" id="about">
			<div class="about__body">
				<h2 class="section-title">About</h2>
				<p class="section-lede">
					I grew up in Tuscaloosa, Alabama, where in my senior year of high school I developed a deep interest in
					physics and astronomy through pop-sci offerings like Neil deGrasse Tyson's <em>Cosmos</em> and Brian Greene's
					<em>The Elegant Universe</em>. I completed my undergraduate studies at the University of Virginia, double
					majoring in Physics and Engineering Science. While there, I worked with Prof. Craig Group building cosmic ray
					veto panels for the Mu2e experiment, and with Prof. Chris Neu on projects tied to the CERN Compact Muon Solenoid
					(CMS) detector.
				</p>
				<p class="about__paragraph">
					I continued my CMS work as a PhD student under Prof. Dan Marlow at Princeton University, culminating in a
					dissertation focused on a search for a pseudoscalar Higgs boson decaying to Zh with ℓℓ + ττ final states. During
					my PhD years I developed a strong interest in machine learning, particularly charged particle track
					reconstruction via graph neural networks. That led to collaborations with Boris Hanin, where we characterized and
					alleviated numerical issues like poor initialization, oversmoothing, and correlation collapse in graph neural
					networks (GNNs).
				</p>
				<p class="about__paragraph">
					My postdoc is focused on furthering the study of numerically stable GNNs—especially graph transformers—by tuning
					them for <em>hyperparameter transfer</em>: models are parametrized so they all agree on the same learning rate.
					I'm now working on several scientific applications for these models, including stellar stream identification in
					large astronomical surveys and jet tagging in collider datasets.
				</p>
				<div class="about__tutorials">
					<h3>Selected tutorials</h3>
					<ul>
						{tutorialLinks.map((item) => (
							<li>
								<a href={item.href} target="_blank" rel="noreferrer">
									{item.label}
								</a>
							</li>
						))}
					</ul>
				</div>
				<div class="about__creative">
					<h3>Creative side quests</h3>
					<p>
						Outside of academia, I'm an avid progressive metal guitarist, writing and recording music with several bands.
						You can check out my public profile here:
					</p>
					<p class="about__creative-link">
						<a href={instagramLink} target="_blank" rel="noreferrer">
							@gagedezoort.music
						</a>
					</p>
				</div>
			</div>
			<div class="about__media">
				<div class="about__image about__image--primary">
					<img src="/images/crash-course-lecture-1.jpg" alt="Gage DeZoort teaching a machine learning crash course" loading="lazy" decoding="async" />
				</div>
				<div class="about__image about__image--secondary">
					<img src="/images/crash-course-lecture-2.jpg" alt="Lecture hall view with students following along" loading="lazy" decoding="async" />
				</div>
			</div>
		</section>

		<section class="section-card highlights">
			<h2 class="section-title">Selected highlights</h2>
			<div class="highlight-grid">
				{highlights.map((item) => (
					<a href={item.url} class="highlight-card" target="_blank" rel="noreferrer">
						<span class="highlight-card__kicker">{item.kicker}</span>
						<h3>{item.title}</h3>
						<p>{item.description}</p>
						<span class="highlight-card__link">Read more →</span>
					</a>
				))}
			</div>
		</section>

		<section class="section-card connect">
			<div>
				<h2 class="section-title">Let's collaborate</h2>
				<p class="section-lede">
					Available for conversations on graph based ML, detector reconstruction, and inclusive physics education.
				</p>
			</div>
			<ul class="connect-list">
				{connections.map((link) => (
					<li>
						<a href={link.href} target={link.href.startsWith('http') ? '_blank' : undefined} rel="noreferrer">
							{link.label}
						</a>
					</li>
				))}
			</ul>
		</section>
	</main>
</Layout>

<style>
	.hero {
		position: relative;
		display: grid;
		gap: clamp(1.8rem, 3.2vw, 3.6rem);
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		align-items: start;
		max-width: var(--max-width);
		margin: 0 auto clamp(3rem, 6vw, 5rem);
		padding: clamp(1.5rem, 3vw, 2.5rem) clamp(0.5rem, 2vw, 1.75rem);
	}

	.hero__heading {
		margin: 0 0 1.2rem;
		font-family: 'Sora', 'Inter', sans-serif;
		font-size: clamp(2.5rem, 5vw, 3.5rem);
		font-weight: 600;
		letter-spacing: -0.02em;
		color: var(--text);
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: clamp(0.6rem, 1vw, 0.9rem);
	}

	.hero__name {
		font-weight: 700;
	}

	.hero__dot {
		font-size: clamp(1.2rem, 2vw, 1.5rem);
		color: var(--accent);
		transform: translateY(-0.05em);
	}

	.hero__dept {
		font-weight: 500;
		letter-spacing: 0.08em;
		color: rgba(243, 244, 249, 0.72);
	}

	.hero__lede {
		margin: 0 0 1.65rem;
		font-size: clamp(1.05rem, 1.2vw, 1.24rem);
		line-height: 1.7;
		color: var(--text-soft);
		max-width: 38rem;
	}

	.hero__actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.85rem;
	}

	.button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.72rem 1.55rem;
		border-radius: 999px;
		font-weight: 600;
		font-size: 0.9rem;
		border: 1px solid transparent;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		transition:
			transform 0.2s ease,
			box-shadow 0.2s ease,
			border-color 0.2s ease;
	}

	.button--primary {
		background: var(--accent);
		color: #1a202a;
		border-color: rgba(255, 255, 255, 0.08);
		box-shadow: 0 12px 28px rgba(15, 12, 9, 0.28);
	}

	.button--primary:hover,
	.button--primary:focus-visible {
		transform: translateY(-1px);
		box-shadow: 0 16px 36px rgba(15, 12, 9, 0.32);
	}

	.button--ghost {
		color: var(--text);
		border-color: var(--border);
		background: transparent;
		box-shadow: 0 10px 24px rgba(8, 12, 20, 0.18);
	}

	.button--ghost:hover,
	.button--ghost:focus-visible {
		transform: translateY(-1px);
		border-color: rgba(255, 255, 255, 0.14);
		box-shadow: 0 16px 32px rgba(8, 12, 20, 0.24);
	}

	.hero__portrait {
		display: flex;
		justify-content: center;
	}

	.hero__frame {
		padding: clamp(0.75rem, 1.8vw, 1.4rem);
		border-radius: 26px;
		background: var(--surface-strong);
		border: 1px solid rgba(255, 255, 255, 0.06);
		box-shadow: 0 24px 60px rgba(8, 12, 20, 0.32);
	}

	.hero__frame img {
		display: block;
		width: clamp(220px, 32vw, 320px);
		border-radius: 18px;
		filter: saturate(1.02);
	}

	.content {
		display: flex;
		flex-direction: column;
		gap: clamp(1.6rem, 3vw, 2.8rem);
		max-width: var(--max-width);
		margin: 0 auto clamp(3.5rem, 7vw, 5rem);
	}

	.about {
		position: relative;
		display: grid;
		grid-template-columns: minmax(0, 1.2fr) minmax(0, 0.9fr);
		gap: clamp(1.5rem, 3vw, 3.5rem);
		align-items: center;
		overflow: visible;
	}

	.about__body {
		display: flex;
		flex-direction: column;
		gap: 1.4rem;
		position: relative;
		z-index: 1;
	}

	.about__tutorials {
		display: flex;
		flex-direction: column;
		gap: 0.6rem;
	}

	.about__tutorials h3 {
		margin: 0;
		font-family: 'Sora', 'Inter', sans-serif;
		font-size: 0.95rem;
		text-transform: uppercase;
		letter-spacing: 0.18em;
		color: rgba(148, 163, 184, 0.9);
	}

	.about__tutorials ul {
		margin: 0;
		padding-left: 1.1rem;
		display: flex;
		flex-direction: column;
		gap: 0.45rem;
		color: var(--text);
	}

	.about__tutorials a {
		color: #7dd3fc;
		font-weight: 500;
		text-decoration: none;
		transition: color 0.2s ease;
	}

	.about__tutorials a:hover,
	.about__tutorials a:focus-visible {
		color: #bae6fd;
	}

	.about .section-lede,
	.about__paragraph {
		color: var(--text);
	}

	.about__creative {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.about__creative h3 {
		margin: 0;
		font-family: 'Sora', 'Inter', sans-serif;
		font-size: 1rem;
		text-transform: uppercase;
		letter-spacing: 0.18em;
		color: rgba(148, 163, 184, 0.9);
	}

	.about__creative-links {
		display: flex;
		flex-wrap: wrap;
		gap: 0.6rem;
	}

	.about__creative p {
		margin: 0;
		color: var(--text);
		line-height: 1.6;
	}

	.about__creative-link a {
		display: inline-flex;
		align-items: center;
		gap: 0.3rem;
		padding: 0.6rem 0.95rem;
		border-radius: 12px;
		border: 1px solid rgba(94, 234, 212, 0.24);
		background: rgba(15, 118, 110, 0.22);
		color: #5eead4;
		font-size: 0.9rem;
		font-weight: 600;
		letter-spacing: 0.04em;
		text-transform: uppercase;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
		text-decoration: none;
	}

	.about__creative-link a:hover,
	.about__creative-link a:focus-visible {
		transform: translateY(-1px);
		box-shadow: 0 18px 32px rgba(13, 148, 136, 0.28);
	}

	.about__creative-links a {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		padding: 0.6rem 0.9rem;
		border-radius: 12px;
		border: 1px solid rgba(94, 234, 212, 0.22);
		background: rgba(15, 118, 110, 0.18);
		color: #5eead4;
		font-size: 0.88rem;
		font-weight: 600;
		letter-spacing: 0.04em;
		text-transform: uppercase;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}

	.about__creative-links a:hover,
	.about__creative-links a:focus-visible {
		transform: translateY(-1px);
		box-shadow: 0 18px 32px rgba(13, 148, 136, 0.25);
	}

	.about__media {
		position: relative;
		display: grid;
		place-items: center;
	}

	.about__image {
		position: relative;
		border-radius: 24px;
		overflow: hidden;
		border: 1px solid rgba(56, 189, 248, 0.16);
		box-shadow: 0 32px 80px rgba(2, 6, 23, 0.5);
		opacity: 0.95;
	}

	.about__image::after {
		content: '';
		position: absolute;
		inset: 0;
		background: linear-gradient(180deg, rgba(2, 6, 23, 0.05), rgba(2, 6, 23, 0.55));
	}

	.about__image img {
		display: block;
		width: 100%;
		height: 100%;
		object-fit: cover;
		filter: saturate(1.05);
	}

	.about__image--primary {
		width: clamp(220px, 28vw, 320px);
		height: clamp(260px, 34vw, 360px);
		transform: rotate(-3deg);
	}

	.about__image--secondary {
		position: absolute;
		right: clamp(-0.5rem, -2vw, -1.8rem);
		bottom: clamp(-1.5rem, -3vw, -2.5rem);
		width: clamp(160px, 20vw, 220px);
		height: clamp(190px, 26vw, 250px);
		transform: rotate(8deg);
		border-color: rgba(168, 85, 247, 0.24);
		box-shadow: 0 28px 60px rgba(76, 29, 149, 0.35);
	}

	.highlight-grid {
		display: grid;
		gap: 1.25rem;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
	}

	.highlight-card {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		padding: 1.6rem;
		border-radius: 20px;
		border: 1px solid var(--border);
		background: var(--surface-strong);
		box-shadow: 0 24px 48px rgba(8, 12, 20, 0.3);
		transition: transform 0.2s ease, box-shadow 0.2s ease;
		color: inherit;
		text-decoration: none;
	}

	.highlight-card:hover,
	.highlight-card:focus-visible {
		transform: translateY(-4px);
		box-shadow: 0 30px 60px rgba(8, 12, 20, 0.4);
	}

	.highlight-card__kicker {
		font-size: 0.78rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.16em;
		color: var(--accent);
	}

	.highlight-card h3 {
		margin: 0;
		font-size: 1.22rem;
		font-weight: 600;
		line-height: 1.3;
		color: var(--text);
	}

	.highlight-card p {
		margin: 0;
		color: var(--text-soft);
		line-height: 1.65;
		font-size: 0.98rem;
	}

	.highlight-card__link {
		font-size: 0.9rem;
		font-weight: 600;
		color: rgba(242, 178, 108, 0.85);
	}

	.connect {
		display: grid;
		gap: 2rem;
		align-items: center;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
	}

	.connect-list {
		display: grid;
		gap: 0.85rem;
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.connect-list a {
		position: relative;
		display: inline-flex;
		align-items: center;
		gap: 0.6rem;
		padding: 0.85rem 2.6rem 0.85rem 1.2rem;
		border-radius: 16px;
		border: 1px solid var(--border);
		background: transparent;
		box-shadow: 0 18px 42px rgba(8, 12, 20, 0.3);
		transition: transform 0.2s ease, box-shadow 0.2s ease;
		font-weight: 620;
		color: var(--text);
	}

	.connect-list a:hover,
	.connect-list a:focus-visible {
		transform: translateX(5px);
		box-shadow: 0 26px 56px rgba(8, 12, 20, 0.38);
	}

	.connect-list a::after {
		content: '→';
		position: absolute;
		right: 1.1rem;
		font-size: 0.95rem;
		opacity: 0.65;
	}

	.connect-list a[target='_blank']::after {
		content: '↗';
		font-size: 0.9rem;
	}

	@media (max-width: 640px) {
		.hero__frame img {
			width: clamp(200px, 70vw, 260px);
		}

		.about {
			grid-template-columns: 1fr;
		}

		.about__media {
			order: -1;
			margin-bottom: 1.25rem;
		}

		.about__image--primary,
		.about__image--secondary {
			position: relative;
			transform: none;
			right: auto;
			bottom: auto;
			width: 100%;
			max-width: 320px;
			margin: 0 auto;
		}

		.about__image--secondary {
			margin-top: 1rem;
		}

		.about__creative-links {
			gap: 0.5rem;
		}
	}
</style>
